[build-system]
requires = [
  "setuptools>=68",
  "wheel",
  "pybind11>=2.12",
  "numpy>=1.26",
]
build-backend = "setuptools.build_meta"

[project]
name = "valuelens-native"
version = "0.1.0"
description = "Native acceleration module for ValueLens"
readme = "README.md"
requires-python = ">=3.10"

